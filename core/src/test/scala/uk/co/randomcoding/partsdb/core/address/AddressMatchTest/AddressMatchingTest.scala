/**
 *
 */
package uk.co.randomcoding.partsdb.core.address.AddressMatchTest

import org.scalatest.FunSuite
import org.scalatest.matchers.ShouldMatchers

/**
 * @author RandomCoder <randomcoder@randomcoding.co.uk>
 */
class AddressMatchingTest extends FunSuite with ShouldMatchers {
  test("Test can match a UK address string") {
    pending
  }

  test("Test can match a UK address as list of strings") {
    pending
  }

}