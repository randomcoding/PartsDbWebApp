/**
 *
 */
package uk.co.randomcoding.partsdb.core.document

import org.scalatest.FunSuite
import org.scalatest.matchers.ShouldMatchers
import uk.co.randomcoding.partsdb.core.document._

/**
 * Tests matching for [[uk.co.randomcoding.partsdb.core.document.DocumentType]]s
 *
 * @author RandomCoder <randomcoder@randomcoding.co.uk>
 *
 * @deprecated("Document Type is now a simple string")
 */
class DocumentTypeMatchTest extends FunSuite with ShouldMatchers {
  /*
  test("Match Invoice Document Type") {
    val d1 = "INV" match {
      case DocumentType(f) => f
      case _ => None
    }
    d1 should be(InvoiceType)
  }

  test("Match Order Document Type") {
    val d1 = "ORD" match {
      case DocumentType(f) => f
      case _ => None
    }
    d1 should be(OrderType)
  }

  test("Match Quote Document Type") {
    val d1 = "QUO" match {
      case DocumentType(f) => f
      case _ => None
    }
    d1 should be(QuoteType)
  }

  test("Match Statement Document Type") {
    val d1 = "STM" match {
      case DocumentType(f) => f
      case _ => None
    }
    d1 should be(StatementType)
  }

  test("Match Delivery Note Document Type") {
    val d1 = "DEL" match {
      case DocumentType(f) => f
      case _ => None
    }
    d1 should be(DeliveryNoteType)
  }

  test("Match Transaction Document Type") {
    val d1 = "TRN" match {
      case DocumentType(f) => f
      case _ => None
    }
    d1 should be(TransactionType)
  }

  test("Match Unknown Document Type") {
    val d1 = "UNK" match {
      case DocumentType(f) => f
      case _ => None
    }
    d1 should be(None)
  }*/

}